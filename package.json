{
  "name": "skygames-fullstack-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "server": "npm --prefix backend start",
    "dev": "npm --prefix frontend run dev",
    "admin": "npm --prefix admin run dev",
    "build": "npm --prefix frontend run build && npm --prefix admin run build"
  }
}
